# BNC GSD Workbench — document-level init
# FreeCAD loads this for both GUI and non-GUI sessions.
# No document object types are registered here; all registration
# happens in InitGui.py and BNCGSDWorkbench.py.
